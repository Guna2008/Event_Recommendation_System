# PostgreSQL connection string
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/event_recommendation?schema=public"

# Port the Express API listens on
PORT=5000

# URL of the deployed/local frontend, for CORS (use * for local dev)
FRONTEND_URL=*
